#include <iostream>

int main()
{
	int cnt = 0;
	int n, kim, im;
	scanf("%d %d %d", &n, &kim, &im);
	while (1)
	{
		kim = kim / 2 + kim % 2;
		im = im / 2 + im % 2;
		cnt++;
		if (kim == im)
			break;
	}

	printf("%d", cnt);
	return 0;
}
