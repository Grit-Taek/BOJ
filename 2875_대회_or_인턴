#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
	int n, m, k;
	scanf("%d %d %d", &n, &m, &k);
	int res = min(min(n / 2, m), (n + m - k) / 3);
	printf("%d", res);
	return 0;


}
