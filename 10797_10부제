#include <cstdio>

int arr[5];
int main()
{
    int a;
    scanf("%d", &a);
    for(int i=0;i<5;i++)
        scanf("%d", &arr[i]);
    int cnt = 0;
    for(int i=0;i<5;i++) {
        if (a == arr[i]) cnt++;
    }
    printf("%d", cnt);
    return 0;


}
