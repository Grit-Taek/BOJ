#include <cstdio>

int main()
{
    int n;
    scanf("%d", &n);
    int aa= 100;
    int bb = 100;
    for(int i=0;i<n;i++)
    {
        int a,b;
        scanf("%d %d", &a,&b);
        if(a > b) bb -= a;
        else if(b > a) aa -= b;
    }

    printf("%d\n%d", aa,bb);
    return 0;
}
