#include<iostream>

int factorial(int n);

int main()
{
    int n = 0;
    scanf("%d", &n);
    int res = factorial(n);
    printf("%d", res);
}

int factorial(int n) {

    if(n == 0) return 1;
    else if(n == 1) return 1;
    return n * factorial(n-1);
}
