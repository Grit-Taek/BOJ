#include <cstdio>
#include <vector>
#include <algorithm>
using namespace std;
vector<double> v;
int main()
{
    int n;
    scanf("%d", &n);
    for(int i=0;i<n;i++)
    {
        double a;
        scanf("%lf", &a);
        v.push_back(a);
    }

    sort(v.begin(), v.end());
    for(int i=0;i<7;i++)
        printf("%.3lf\n", v[i]);

    return 0;
}
