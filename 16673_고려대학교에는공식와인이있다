#include <cstdio>

int main()
{
    int c,k,p;
    scanf("%d %d %d", &c,&k,&p);
    int res = 0;
    for(int i=1;i<=c;i++)
    {
        int x = k * i + p *i*i;
        res += x;
    }
    printf("%d", res); 
    return 0;
}
