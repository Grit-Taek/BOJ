#include <iostream>
#include <string>
using namespace std;

int main()
{
    string str;
    cin >> str;
    int idx = -1;
    for(char c='a';c<='z';c++)
    {
        idx = str.find(c);  // find는 string의 함수, 그 시작위치를 반환한다.
        cout << idx << " ";
        idx = -1;
    }

    return 0;

}
