#include <iostream>

int main() {
    int a, b, c;
    scanf("%d %d %d", &a,&b,&c);

    int res = c-b;
    if(res <= 0)
        printf("-1");
    else {
        int num = a / res + 1;
        printf("%d", num);
    }

    return 0;


}
