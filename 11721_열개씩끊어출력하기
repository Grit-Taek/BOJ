#include <iostream>
#include <string>

using namespace std;

int main()
{
    string str;
    cin >> str;
    int i = 0;
    while(i < str.size())
    {

        if(i % 10 == 0 && i != 0)
            cout << "\n";
        cout << str[i];
        i++;
    }

    return 0;
}
