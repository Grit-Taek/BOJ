#include <iostream>

int main()
{
	int n;
	scanf("%d", &n);
	int binary;
	int cnt = 0;
	for (int i = 0; i < n; i++)
	{
		scanf("%1d", &binary);

		if (binary == 1)
			cnt++;
	}

	printf("%d", cnt);
	return 0;
}
